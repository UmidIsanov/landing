export enum ApplicationStatuses {
  IN_MODERATION = 11,
  ACTIVE = 22,
  DISABLED = 33,
  REJECTED = 44,
  UPDATE_MODERATION = 55,
}

export enum ApplicationNamesOfStatuses {
  ACTIVE = "ACTIVE",
  IN_MODERATION = "IN_MODERATION",
  DISABLED = "DISABLED",
  REJECTED = "REJECTED",
  UPDATE_MODERATION = "UPDATE_MODERATION",
}

export enum ApplicationTypes {
  TUTOR = 1,
  HIRER = 2,
}
